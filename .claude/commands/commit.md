Read AGENT/changelog and summarize its contents into a single conventional git commit message (e.g. "feat: ...", "fix: ...", "chore: ..."). Then:

1. ! git add .
2. Use the summarized message to run: ! git commit -m "<message>"
3. ! git push origin master
4. Overwrite AGENT/changelog with a single empty line using the Write tool.

Do not ask for confirmation. Execute all steps in order.
