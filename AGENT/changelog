2026-04-02 — Phase 3 Task 1: created SplashScreen with radial ember glow, SVG runner icon, grain texture overlay, and headline/subtext; wired into App.tsx at step 0
2026-04-02 — Phase 3 Task 2: added word-by-word headline animation (fade + 8px slide-up, 60ms stagger per word, 200ms duration); subtext fades in after last word
2026-04-02 — Phase 3 Task 3: added "Build my plan →" CTA button to SplashScreen; ember background, fades in after subtext, calls goNext() via onStart prop
2026-04-02 — Fix: splash screen is now truly full-screen (no top bar); STRIDE logo placed at upper-left inside SplashScreen; top bar only shown on steps 1+
